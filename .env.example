# 复制本文件为 .env，并按需修改密码后再部署

# MySQL root 密码
MYSQL_ROOT_PASSWORD=change_this_root_password

# 业务数据库与账号
DB_NAME=bookmarks
DB_USER=bookmarks
DB_PASSWORD=change_this_password

# 前端对外端口（默认 8080，避免与服务器上其它服务的 80 端口冲突）
APP_PORT=8080
